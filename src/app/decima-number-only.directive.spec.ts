import { DecimaNumberOnlyDirective } from './decima-number-only.directive';

describe('DecimaNumberOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimaNumberOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
